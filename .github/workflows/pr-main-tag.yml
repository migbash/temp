# -----------------------------
# NOTE: https://github.com/marketplace/actions/github-tag-bump
# example 2: on merge to master/main
# -----------------------------

name: Bump version [TEST]

on:
  pull_request:
    types:
      - closed
    branches:
      - dev
      
jobs:
  build:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-22.04
    steps:

    # NOTE: CUSTOM IMPLEMENTATION
    - name: 'Checkout source code'
      uses: 'actions/checkout@v3'
    - name: Dump GitHub context
      run: |
        echo "complete Github Context ${{ toJson(github) }}"
        echo "The title of PR is: ${{ github.event.pull_request.title }}"

    # - name: Git config
    #   run: |
    #     git config --global user.email "${{ secrets.GH_ORG_EMAIL }}"
    #     git config --global user.name "${{ secrets.GH_ORG_NAME }}"
    
    # - name: Apply version bump (major)
    #   if: contains(github.event.pull_request.labels.*.name, 'major')
    #   run: npm version major
    - name: Apply version bump (minor)
      if: "contains(github.event.pull_request.title, 'minor')"
      # condition: ${{ containsValue([minor], 'github.event.pull_request.title') }}
      run: echo "MINOR detected!" 
    # - name: Apply version bump (patch)
    #   if: contains(github.event.pull_request.labels.*.name, 'patch')
    #   run: npm version patch

    # - name: Git push version bump
    #   run: git push origin dev --follow-tags --force
    # - id: set-version
    #   name: Output version change
    #   run: npm pkg get version